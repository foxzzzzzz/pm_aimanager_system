# 变更记录与里程碑

## 2026-08-15

### 本地测试环境重置

- 新增 PowerShell 与 Linux Shell 两个带显式确认参数的本地 Docker Compose 重置脚本，清除 PostgreSQL、Redis 和 MinIO 数据卷后重建服务并执行数据库迁移，方便重复导入验收。
- Linux 脚本增加生产 Compose 验收数据重置模式：仅清 PostgreSQL、Redis 和 MinIO，保留 Caddy 配置及TLS证书，避免使用 `down -v` 误删HTTPS运行依赖。

### Web问题登记弹窗布局

- 将R/A/C/I、严重程度和预计完成日期从单行固定宽度控件改为响应式两列布局，避免在默认弹窗宽度下横向溢出；日期控件占满本列。
- 补充管理端回归断言，确保上述六个字段均渲染在半宽栅格列内。

### 项目创建与导入身份一致性

- 管理后台新增未发布项目的编辑和删除入口；服务端禁止修改或删除任何已发布版本的项目，避免破坏历史可追溯性。
- Excel导入新增“从Excel新建项目”路径：仅在模板解析成功后创建项目和待确认导入记录；项目编号已存在时明确提示选择已有项目导入。
- 既有项目导入从仅校验项目编号扩展为同时校验项目名称，阻止手工新建信息与Excel身份不一致导致的双名称。
- 补充项目基本信息修改、删除保护、Excel自动建项目、重复编号和名称不一致的API回归用例。

## 2026-08-14

### 邀请码直接绑定临时方案

- 新增生产默认关闭的 `WECHAT_ALLOW_INVITATION_ONLY_BINDING` 开关；启用后，未设置预期手机号的邀请码可直接绑定已微信登录的账号，设置了预期手机号的邀请码则进入项目经理审核，避免在无法验证号码时自动放宽该核验。
- 小程序增加由 `runtimeConfig.allowInvitationOnlyBinding` 控制的“使用邀请码直接绑定”入口，保留原有手机号授权入口以便企业主体认证完成后恢复手机号核验。
- 补充邀请直接绑定开关、自动绑定及待审核的 API 回归用例，并记录短信和企业主体认证完成后的恢复事项。

## 2026-08-13

### 腾讯云生产部署自动化

- 新增生产Compose覆盖、Caddy自动HTTPS入口、生产环境模板及Linux交互式初始化/部署脚本；生产服务端口均保持回环绑定，只有Caddy开放80/443。
- 初始化脚本自动生成并保护数据库、MinIO、管理员令牌和手机号安全密钥，仅要求操作者输入域名、微信和腾讯云短信的外部配置；部署脚本自动构建、迁移、HTTPS健康检查与生产配置门禁检查。
- 小程序真实HTTPS地址与关闭开发登录保留为服务器部署成功后的显式配置步骤，避免在未知域名前把局域网测试配置误发布。
- 初始化默认跳过腾讯云短信配置并保持短信关闭；完成企业认证、签名和模板后可重跑同一脚本选择配置短信，无需维护重复的部署脚本。

### 小程序看板操作样式统一

- 项目看板“查看项目资料”“更新进度”及重点问题新增/删除审批的批准、驳回按钮统一为深绿色主按钮与白底绿色描边次按钮；“作废问题”弹窗的原因输入框默认留空，避免用户先删除预填文字。
- “我的任务”和项目看板的筛选标签统一风险颜色：近期使用橙色，逾期使用红色，选中后保持白字绿色主状态。

### Phase F5 M04真机验收缺陷修复

- 小程序重点问题列表和新增审批卡片改为预先生成R/A/C/I展示文本，避免WXML运行时方法导致人员缺失；问题编辑支持维护A/C/I并继续执行正式团队成员校验。
- 新增/删除审批卡片可跳转到独立审批详情页，完整展示问题描述、影响、RACI、严重程度、预计完成时间和删除原因；Web与小程序统一补齐提交时间及预计完成时间。
- “我的”任务纳入当前成员承担R或A的正式重点问题，并与里程碑共用待办、近期、逾期和已完成分类。
- 底部“消息”增加未读数量角标；“项目”入口及项目卡片增加当前账号待处理审批数量，审批或读消息后自动刷新。
- 审批卡片将RACI独立换行；正式重点问题编辑允许原R从当前团队重新指定R；“我的”重点问题改为跳转“问题”页并自动定位、高亮目标记录。
- 修复Web项目看板把计划状态“不适用”的里程碑归入待办的问题；API看板统一排除不适用任务，Web同时增加防御性过滤，与小程序保持相同分类口径。
- Phase F5自动验收、Docker运行态及M01至M07人工验收完成；正式微信订阅消息/短信渠道配置作为外部部署事项转入TodoList。

## 2026-08-12

### Phase F4审批界面与结果通知

- 小程序仅为当前项目经理加载重点问题新增/删除审批队列，无待审项时展示明确空态；审批按钮增加串行加载、禁用及成功/失败反馈。
- Web重点问题新增/删除审批增加操作中状态和成功提示，提交删除申请后明确提示仍需项目经理审批。
- 移动端提交的新增/删除申请在批准或驳回后向提交人生成站内消息，消息中心增加四类审批结果的中文标签。
- 待新增申请仍不进入正式问题与每日通知；待删除申请仍保留原问题有效，只有批准关闭后才停止后续问题提醒。
- 全量检查通过：API 119项、管理端17项、小程序54项测试，以及Ruff、mypy、TypeScript、管理端生产构建和Compose配置校验。

### Phase F3重点问题删除审批

- 新增独立 `issue_delete_proposals` 审批数据模型和 `0011_issue_delete_proposals` 迁移；删除申请与正式问题状态分离。
- Web与小程序删除问题改为提交申请；待审期间原问题状态和版本不变，继续参与正式列表、风险统计与既有通知扫描。
- 项目经理可在小程序项目看板批准或驳回，Web管理员可在“问题删除审批”页签处理；批准后问题版本递增并关闭，驳回后原问题保持不变。
- 阻止同一问题重复提交待审删除申请，并使用原问题版本进行审批并发校验；申请、批准、驳回及最终关闭均写入审计。
- 全量检查通过：API 118项、管理端17项、小程序54项测试，以及Ruff、mypy、TypeScript、管理端生产构建和Compose配置校验；SQLite迁移完成全链路升级、回退和再次升级。

### Phase F2重点问题新增审批

- 新增独立 `issue_create_proposals` 审批数据模型和 `0010_issue_create_proposals` 迁移；待审申请与正式问题隔离，历史问题继续作为已批准正式数据使用。
- Web与小程序登记重点问题统一先提交新增申请，R可选择项目当前正式团队的任一成员，A/C/I继续使用既有团队成员校验。
- 项目经理可在小程序项目看板批准或驳回，Web管理员可在“问题新增审批”页签处理；批准时才创建正式问题，驳回不产生问题。
- 申请提交、批准、驳回及正式问题创建均写入审计；审批前申请不进入问题列表、看板风险统计或每日问题通知。
- 全量检查通过：API 117项、管理端16项、小程序53项测试，以及Ruff、mypy、TypeScript、管理端生产构建和Compose配置校验；SQLite迁移完成全链路升级、回退和再次升级。

### Phase F1项目经理权限与节点审批

- Excel团队表必须且只能包含一名“项目经理”；正式快照保留规范化经理身份，管理端数据变更同样不能发布缺失或重复经理的版本。
- 绑定成员角色优先从团队表继承项目经理身份；重新导入发布后自动向新任经理转移权限，节点RACI保持原样。
- 增加 `0009_project_manager_permissions` 数据迁移，按当前正式版本修复既有绑定成员权限，包括当前被误映射为节点A的项目经理。
- 普通节点A不能批准或驳回自己的申请；项目经理可查看和审批项目内全部节点申请，包括本人提交的申请。R=A且本人不是项目经理时由项目经理审批，R≠A时由独立节点A或项目经理任一审批。
- 小程序项目经理可查看和处理项目内全部待审节点申请；Web管理员继续拥有项目经理审批权限。
- 全量检查通过：API 115项、管理端15项、小程序52项测试，以及Ruff、mypy、TypeScript和管理端生产构建。
- 修复小程序将本人提交的待审节点申请直接过滤的问题；普通成员本人申请显示“待他人审批”，项目经理本人申请显示“可审批”。Web与小程序审批记录同时增加本地化提交时间。

### Web按项目看板

- 管理后台增加独立“项目看板”菜单，按当前选择项目展示待办、近期、逾期和已完成节点，卡片包含计划日期及R/A/C/I，并提供当前项目的重难点问题风险摘要。
- 扩展既有项目看板API，兼容保留原统计字段并增加服务端业务日期、已分类节点任务和问题摘要；项目权限仍由既有项目成员关系隔离。
- 提取API与小程序共用的里程碑风险函数，Web直接使用服务端分类，避免浏览器本地日期、时区或重复实现造成跨端偏差。
- Web看板提供项目数据与变更、问题管理入口，写操作继续进入既有变更集、乐观锁、权限与审计流程，不在看板内直接覆盖正式数据。
- API 108项、管理端15项、小程序51项测试以及Ruff、mypy和TypeScript检查通过。

### 重难点问题RACI与风险提醒

- 问题模型在原责任人R之外增加必填A及可选多选C/I；API统一校验所有RACI姓名均来自当前正式项目成员，管理后台支持完整维护，小程序登记时支持选择并在列表展示完整RACI。
- 问题列表由服务端业务日期派生待办、近期、逾期、已完成风险状态；近期沿用外部配置的14天窗口，今天和第14天均计入近期，第15天归入待办。
- 既有每日扫描继续统一遍历问题：提前提醒发送R，到期及逾期发送R+A并去重，已解决/已关闭停止提醒，C/I默认不接收高频提醒。
- 增加 `0008_issue_raci` 迁移，旧问题自动保留原责任人为R并回填为A，C/I初始化为空；SQLite完成全链路升级、回退、再次升级及旧数据回填验证。
- API 106项、管理端14项、小程序51项测试全部通过；Ruff、mypy、管理端和小程序TypeScript检查通过。

### 小程序“我的”R/A任务

- 新增移动端个人任务聚合接口，按有效成员绑定隔离项目，仅返回本人承担R或A的里程碑；同一节点同时承担R/A时合并为一条任务并保留双角色标识。
- 小程序底部导航增加“我的”，按项目分组展示本人任务，提供待办、近期、逾期和已完成四类筛选，并可进入对应项目看板。
- 个人任务风险使用服务端业务日期计算；小程序近期窗口从外部配置读取，默认14天，与通知默认3天提前量保持独立。
- 新增未绑定空结果、R/A去重、页面结构和四栏窄屏布局回归；API相关17项测试、小程序50项测试及TypeScript检查通过。

### RACI个人任务、问题协同与跨端看板（规格阶段）

- PRD升级至v1.5：规划小程序“我的”入口，按项目聚合当前成员承担R/A的里程碑并复用待办、近期、逾期和已完成分类。
- 将问题现有责任人明确为R，新增必填A及可选多选C/I；问题业务状态保持不变，风险分类由预计完成日期和完成状态派生。
- 审计确认现有工作日每日扫描已覆盖里程碑与问题；小程序14天风险窗口和通知默认3天提前量继续独立配置，避免改变现有通知强度。
- 规划管理后台按项目看板，并拆分为规格审计、小程序个人任务、问题RACI、Web看板和联合验收五个独立阶段。

### 小程序项目风险概览

- “近期”时间窗口由3天扩展为后台业务日期当天至未来14天，包含当天和第14天；逾期仍定义为计划结束日期早于业务日期的未完成有效节点。
- 看板固定显示待办、近期和逾期筛选，已完成与全部通过“更多筛选”操作表访问；有逾期时使用红色筛选语义。
- 筛选按钮改为兼容微信原生按钮的弹性宽度布局，前三项等宽、“更多筛选”略宽，避免小屏横向溢出和文字裁切。
- 移动项目列表接口增加后台业务日期及当前版本节点计划/RACI；项目首页按项目展示逾期、近期节点、截止日期与R/A/C/I，逾期使用红色告警样式。
- 小程序49项测试、TypeScript类型检查、移动工作流13项API测试及Ruff检查通过。

## 2026-08-11

### 小程序UI优化阶段5

- 项目、看板、项目资料、问题和消息页面新增可持续展示的加载失败状态及“重新加载”操作，网络恢复后无需退出页面即可继续使用。
- 消息列表增加未读圆点和文字标识，已读操作使用接口返回数据局部更新，避免额外刷新；底部项目、问题、消息导航增加普通态和选中态图标。
- 看板“查看项目资料”和“更新进度”改为靠右排列的绿色椭圆操作按钮；底部导航降低未选中项颜色权重并提高选中绿色饱和度。
- 资料和进度操作改为与“批准变更”一致的微信原生主按钮样式，移除方向箭头；同时清除默认自动外边距，使资料、进度及审批操作组均贴齐卡片右侧。
- “待我审批”保持原有胶囊结构，改用橙红文字和浅橙背景，强化待处理审批提醒且不与驳回危险操作混淆。
- 新增错误恢复、消息未读、导航资源和选中颜色自动化测试，小程序45项测试及TypeScript类型检查通过。

### 小程序UI优化阶段4

- 项目资料的团队成员和RACI页签新增本地关键词检索，分别覆盖姓名/角色/备注及节点编号/名称/输出/责任成员，并提供结果计数和空状态。
- 过长产品规格详情按外部配置的字符阈值默认折叠，支持逐条展开和收起；RACI筛选后继续保持增量展示。

### 小程序UI优化阶段3

- 节点更新页改为更新类型、日期、原因和提交审批的单一主流程，补齐固定标签、必填标识、分段选择和统一日期选择器。
- 自然语言预填默认收起为“一句话快速填写”辅助入口；识别成功后自动回到主表单，并在提交前明确要求核对和确认结果。

### 小程序UI优化阶段2

- 重难点问题页默认收起登记表单，优先展示问题数量和列表；新增或编辑时展开表单并自动滚动到填写区域，保存或取消后恢复列表视图。
- 问题表单补齐固定字段标签、必填标识、只读责任人和统一选择器；问题卡片操作改为紧凑的编辑、开始处理和作废按钮组。

### 小程序UI优化阶段1

- 项目看板的“查看项目资料”和“更新节点”改为带分隔线和方向提示的统一操作行，避免默认小按钮在卡片中孤立悬浮。
- 全局卡片缩减内边距、间距、圆角和阴影，列表首屏可展示更多业务内容；页面增加底部安全区适配。
- 项目列表和项目资料移除与原生导航栏重复的页面标题，保留必要的版本与范围说明。

### 小程序产品规格检索

- 项目资料的产品规格页签新增常驻关键词搜索，覆盖规格、分类、配置、核心信息、型号和备注，并实时显示匹配数量。
- 搜索结果继续使用增量展示，关键词变化或清空时自动重置分页，未匹配时显示明确空状态。

### 小程序已绑定成员入口修复

- 登录页在已有登录会话时自动查询项目权限，为已绑定成员显示“进入我的项目”入口；普通启动时不再继续展示无必要的成员绑定表单。
- 已使用、已过期、被重新生成或不存在的邀请码统一展示明确的失效说明，避免用户把一次性邀请码失效误判为构建或登录故障。

### 后台启动脚本

- 新增 `scripts/start-backend.ps1`，支持在 Docker 环境中后台拉起完整 Compose 服务、等待健康检查、执行数据库迁移并输出管理端与 API 访问地址。
- 新增功能一致的 Linux Shell 启动脚本 `scripts/start-backend.sh`。
- 新增 PowerShell 与 Linux 脚本的启动顺序和动态端口解析回归测试。

## 2026-08-10

### 小程序 Phase 1–6 审查修复

- 审批与驳回统一使用项目行锁并刷新提案状态，避免并发解析产生正式版本与提案终态不一致；小程序成员只能创建本人负责的问题。
- 延期自然语言预填保留独立开始和结束日期；网络传输失败使用同一幂等键自动重试，问题变更使用接口返回值直接更新页面，避免写入成功但刷新失败导致重复操作。
- 移动端看板返回后台业务日期，消息时间统一输出UTC；小程序兼容无时区时间戳、重新进入和下拉刷新，并对审批增加发布确认。
- 邀请微信Token增加缓存，小程序码严格校验图片格式；微信入口生成移到邀请事务提交之后，幂等记录不再保存Base64图片。
- 项目资料接口改为字段白名单，小程序规格与RACI采用可配置的增量展示；开发者工具只保留`apps/mini-program`一个导入根目录。

### 小程序呈现统一（阶段6）

- 新增共享呈现层，统一计划状态、问题严重程度和消息类型的中文标签；日期缺省统一显示为“—”，消息时间按外部配置的业务时区偏移展示到分钟。
- 项目、看板、项目资料、问题和消息页面补齐加载、空数据及加载失败状态；项目切换和消息刷新期间不再短暂展示旧数据。
- 看板计划窗口不再暴露`not_applicable`等后端枚举，问题卡片不再显示`critical`等英文值，节点更新按钮改为清晰的中文动作名称。
- 新增枚举、时区、计划窗口及页面状态回归测试；小程序18项测试和TypeScript检查通过。

### 小程序邀请入口（阶段5）

- 成员邀请令牌调整为适配微信小程序码`scene`限制的32字符高熵令牌，并保持登录会话令牌原有强度；邀请有效期、入口页面、目标版本和小程序码宽度统一由外部配置控制。
- 配置正式微信AppID/AppSecret后，邀请接口生成可直接打开的URL Link和真实小程序码；本地未配置时保留可复制的小程序路径并返回明确降级原因，不伪造二维码或阻断邀请创建。
- 管理后台邀请弹窗展示可复制链接和小程序码；小程序登录页同时识别URL Link的`invitation`参数和小程序码的`scene`参数。
- 新增微信接口请求体、未配置降级、邀请长度、后台展示和扫码入口回归测试；后端95项、管理端14项、小程序14项测试通过，Ruff、mypy、前端TypeScript及管理端生产构建检查通过。

### 小程序只读项目资料（阶段4）

- 新增仅限已绑定项目成员访问的移动端项目核对接口，复用正式版本快照并返回产品规格、脱敏团队成员及里程碑RACI；未绑定用户无权读取。
- 项目看板增加“查看项目资料”入口，小程序以产品规格、团队成员和RACI三个只读标签页展示数据，不提供输入、修改或发布操作。
- 新增移动端权限与敏感字段回归测试以及小程序页面结构测试；后端93项、移动工作流13项、小程序13项测试通过，Ruff、mypy及小程序TypeScript检查通过。

### 小程序节点分类（阶段3）

- 项目看板增加“待办、近期、逾期、已完成、全部”节点筛选及实时数量，默认聚焦未完成且适用的待办节点。
- “近期”窗口由小程序运行配置统一控制，当前为计划结束日前3天（含当天）；逾期、已完成及不适用节点不会混入待办计数。
- 新增固定日期边界回归测试，覆盖当天、近期窗口末日、逾期、已完成和不适用节点；小程序12项测试及TypeScript检查通过。

### 小程序交互补强（阶段2）

- 新增移动端节点变更驳回接口和A角色权限回归测试；小程序待审批卡片支持批准或填写原因驳回，并对处理操作防重。
- 重难点问题支持负责人编辑描述、影响、严重程度、预计完成日期和状态，禁止在小程序编辑时改派负责人；非本人负责问题不显示编辑、处理和作废操作。
- 未配置正式订阅消息模板时禁用微信提醒按钮并显示配置说明，避免将占位模板ID传给微信接口。
- 后端92项、移动工作流12项、小程序10项测试通过，Ruff、mypy及前后端TypeScript检查通过。

### 小程序交互补强（阶段1）

- 节点完成和延期表单增加前置字段校验、明确错误提示及提交防重；AI/规则预填后必须显式确认，确认后的关键字段再次变化会重新触发确认门禁。
- 问题登记增加必填校验、创建及状态操作防重，并在成功后完整清空表单；问题页显示当前项目并提供选择和切换入口。
- 选择项目时同步保存项目编号与名称，避免问题Tab仅依赖不可见项目ID；新增表单验证及页面交互回归测试，小程序9项测试和TypeScript检查通过。

### 小程序真机调试修复

- 修正普通小程序工程配置，启用TypeScript编译插件并补充有效sitemap规则，解决首页脚本缺失和真机sitemap校验失败。
- 将请求核心模块从真机不兼容的`.mjs`调整为`.js`并同步类型声明，解决运行时将模块错误解析为`request-core.mjs.js`导致页面空白的问题。
- 小程序开发配置改用当前局域网API地址并关闭本地合法域名校验，支持真机访问本地Docker API；新增配置与模块后缀回归断言。
- 小程序5项测试及TypeScript类型检查通过，并完成真机登录、成员绑定、项目列表、项目看板、节点更新、问题和消息页面冒烟验证。

### 生产可靠性修复与性能优化

- Excel解析改为可超时终止的独立进程，新增允许扩展名、ZIP条目数和解压后总大小配置，避免异常工作簿阻塞API或耗尽内存。
- 幂等记录新增请求摘要并增加`0007_reliability_hardening`迁移；相同幂等键的不同请求返回409，并发唯一键竞争回读已提交结果。
- 问题更新和版本化关闭改为数据库条件更新，修复并发请求均通过旧revision校验造成的静默覆盖。
- 外部通知在调用微信或短信前先持久化`pending`，发送结果立即落库；扫描批量预取项目版本、问题、成员绑定、用户、授权和外部通道计数。
- 微信订阅消息发送器缓存有效access token；LLM对408、429和5xx按配置执行带抖动退避并支持`Retry-After`。
- 手工扫描和Celery任务统一使用配置业务时区；小程序可展示结构化冲突消息。
- 管理端Docker镜像改为多阶段生产构建并由Nginx提供静态文件；拆分React、图标和表格运行时，最大构建chunk由756.75KB降至490.53KB且无循环chunk告警。
- 新增8项后端回归测试和1项小程序测试；后端91项、管理端14项、小程序5项测试全部通过。

## 2026-08-07

### Phase 5 真实试点与生产准备（进行中）

- PRD升级至v0.9，确认Excel导入、管理后台和小程序三端统一的版本化更新；后台开放规格、成员、里程碑与RACI增删改查，小程序继续按R/A/C/I限权，所有变更经差异确认后发布新版本。
- 新增管理端“项目核对”页面和只读核对API，展示产品规格关键参数、脱敏团队成员及合并角色、里程碑计划和RACI，并支持仅查看待定节点。
- 修复“项目核对”表格切换每页条数后仍固定显示10条的分页问题，并增加回归测试。
- 新增项目数据变更集迁移与API，后台可对规格、成员、里程碑、计划和RACI执行受限增删改，预览差异后发布不可变新版本；成员删除或改名会同步处理RACI引用。
- 后台问题页补齐编辑和版本化删除，小程序补齐本人负责问题的作废接口与交互；删除均要求原因并保留历史审计。
- 将最终Lyra-PRO文件通过正式导入流程发布为本地v3，项目总览更新为70条规格、19名唯一成员、24个里程碑和3个待定节点；历史版本保持不变。
- 修复Docker API未显式设置可写导入暂存目录导致真实文件上传500的问题，并增加Compose回归测试。
- 本地管理员令牌在页面验收输出中意外暴露后已立即单独轮换；配置脚本新增选择性轮换能力，避免同时更换手机号HMAC和加密密钥。
- 使用最终Lyra-PRO文件完成隔离导入和发布，解析得到70条产品规格、19名唯一成员、24个里程碑及3个计划版本；保留3个待定节点供项目经理人工核对。
- 修复同一人员在团队表承担多个岗位时被误判为重复成员的问题：角色合并，手机号或邮箱冲突仍拒绝导入，并补充回归测试。
- 新增生产配置检查与通知运行状态API，覆盖开发登录、占位配置、密钥强度、CORS、外部投递失败、滞留任务和未绑定接收人；管理端展示生产准备告警。
- 正式微信订阅模板、短信应用参数及生产CORS均支持环境变量注入，不把生产凭据写入配置文件。
- 新增PostgreSQL和MinIO备份、校验、一次性恢复演练及全服务运行检查脚本；本地已完成一次真实备份恢复演练。
- 新增Phase 5试点检查表、验证记录和生产运维手册。正式通道验证、项目经理人工核对及连续两周试运行仍待完成。
- Phase 5.1回归通过：后端83项、管理端14项、小程序4项测试全部通过；Ruff、mypy、TypeScript和管理端生产构建通过；SQLite迁移往返成功，PostgreSQL已增量升级至`0006_change_sets`，Docker API与管理端健康。

### Phase 4 自动提醒与外部通知

- 新增工作日每日扫描和每周一14天摘要，覆盖未来3天、当天到期、逾期持续提醒及逾期第2/5天向A角色和项目经理升级；已完成节点、已解决/关闭问题自动停止提醒。
- 新增Celery worker/beat与Redis调度，规则时间、窗口、升级阈值、外部通道单日限额均由YAML配置。
- 新增通知投递记录和业务日幂等键；站内消息始终优先落库，微信订阅消息按用户主动授权次数消费，关键事件在微信不可用或失败时按配置降级腾讯云短信。
- 手机号绑定时同时保存HMAC、掩码和AES-256-GCM密文，密文包含版本封套；历史用户因Phase 3.1已删除明文，需重新绑定手机号后才具备短信能力。
- 新增管理端通知诊断页、手工扫描和失败外部投递重试；未绑定移动身份的升级接收人以`skipped`记录，避免静默漏报。
- 小程序消息中心新增主动订阅按钮，仅在`wx.requestSubscribeMessage`返回接受后向后台登记一次授权。
- 新增`0005_phase4`迁移及通知规则、加密防篡改、幂等、失败降级、重试、周报、订阅授权测试。

### Phase 4 验证

- 后端66项、管理端7项、小程序4项测试通过；Ruff、mypy、TypeScript和管理端生产构建通过。
- SQLite完成`base -> 0005 -> base -> 0005`往返迁移；PostgreSQL从`0004_phase31`增量升级至`0005_phase4`。
- Docker API、管理端、PostgreSQL、Redis、MinIO健康，通知worker连接Redis并注册每日/每周任务，beat正常运行。

### Phase 4 前置设计与本地配置

- 确认短信兜底采用AES-GCM密文、HMAC摘要和掩码三态手机号存储，新增外部密钥 `PHONE_ENCRYPTION_KEY`，仅通知服务可解密。
- 增加 `scripts/configure-local-secrets.ps1`，自动生成并默认保留本地管理员令牌、手机号HMAC密钥和手机号加密密钥。
- 本机 `.env` 已生成且受 `.gitignore` 保护；Docker API和管理端使用持久化本地配置重启后健康，Bearer认证验证通过。

### Phase 3.1 修复与优化

- 管理端取消可伪造的 `X-Actor-Id`，改为运行时Bearer令牌和服务端管理员身份映射。
- 修复旧基线提案误批准、历史Excel假发布、冲突状态回滚、倒置日期和仅重新保存文件产生伪差异的问题。
- 项目版本发布与提案审批增加项目行锁、基线和目标字段校验；发布团队变更时自动撤销离组成员权限。
- 手机号改为服务端密钥HMAC和掩码存储；限制同一用户在同一项目只能绑定一个成员身份。
- 补齐管理端成员邀请，以及小程序A角审批、问题状态更新和消息已读闭环。
- OpenAI兼容客户端接入外部重试与结构化输出模式配置，支持严格Schema失败后降级JSON模式，并在发送前脱敏手机号。
- 增加 `0004_phase31` 迁移和前后端行为测试；迁移既有版本为业务快照哈希，并修复PostgreSQL与SQLite的手机号掩码SQL差异。升级时会安全撤销无法无损转换的未完成旧邀请，项目经理可重新生成。

### 验证

- 后端54项、管理端7项、小程序4项测试全部通过；Ruff、mypy、TypeScript和生产构建通过。
- PostgreSQL从 `0003_phase3` 增量升级到 `0004_phase31`，确认手机号明文字段已删除。
- Docker五个服务健康；管理API验证缺失令牌401、错误令牌403、有效令牌200。
- 浏览器验证管理员令牌门禁、项目看板、成员绑定掩码及邀请入口正常。

## 2026-08-06

### 新增

- 建立AI项目管理系统PRD v0.1。
- 确定后台为唯一项目管理中心，小程序作为输入、看板和个人通知入口。
- 确定MVP仅支持Lyra-PRO固定格式 `.xlsx`，并采用模板版本对应解析器。
- 确定Excel和小程序双更新渠道、差异确认和冲突处理规则。
- 确定OpenAI兼容LLM接口、微信订阅消息和短信降级边界。
- 建立Phase 0至Phase 5实施计划和测试先行清单。
- 初始化Git仓库、Python 3.12 API、React管理端和原生TypeScript小程序骨架。
- 增加外部配置示例、Docker Compose、CI、依赖锁和统一启动/检查脚本。
- 增加脱敏Lyra模板1.0夹具及人工核对的预期数据。
- 完成Phase 1 CanonicalProjectDraft、计划日期状态、成员、规格、里程碑和RACI领域模型。
- 增加外部YAML模板清单、ParserRegistry和LyraTemplateV1Parser，固定支持 `lyra_project_spec/1.0`。
- 增加XLSX扩展名/文件类型/模板指纹/必要结构/日期/RACI语义校验和结构化导入报告。
- 增加字段级语义Diff、Project/ProjectVersion/ImportRecord数据库模型及 `0001_phase1` Alembic迁移。
- 完成Phase 2项目创建、Excel导入/差异/取消/发布、版本历史和项目看板API。
- 增加项目成员权限、写操作幂等、乐观并发、进度变更提案、问题管理和审计日志。
- 增加 `0002_phase2` 数据库迁移，并将Docker原始规格书存储接入MinIO/S3兼容接口。
- 完成React管理端项目总览、导入发布、版本历史、问题与审计页面，并按页面懒加载。
- 完成Phase 3微信会话、项目邀请、手机号绑定、异常审核、移动端项目隔离和站内消息API。
- 完成R角色节点完成/延期提案、A或项目经理审批发布、项目经理驳回及不可变版本写入闭环。
- 完成原生小程序登录绑定、我的项目、项目看板、节点更新、问题登记和消息中心页面。
- 增加OpenAI兼容Chat Completions客户端及严格JSON Schema输出；未配置LLM时保留本地规则预填和结构化表单。
- 管理端增加成员绑定审核和移动端变更提案审批/驳回视图。
- 调整API Dockerfile依赖分层，使源码变更可复用已锁定的Python依赖层。

### 验证

- 后端7个测试、管理端1个测试和小程序1个测试通过。
- Python lint/mypy、管理端和小程序TypeScript检查、管理端生产构建通过。
- API `/health` 与管理端静态产物本地HTTP冒烟验证通过。
- API、管理端、PostgreSQL、Redis和MinIO五个容器均达到healthy。
- 管理端浏览器渲染通过，关键文案可见且无控制台错误。
- 修复Docker构建包源隐式依赖和本机Redis端口冲突；增加锁定运行时依赖、构建重试、可配置宿主机端口和全服务健康检查。
- Phase 1后端27个测试、Ruff、mypy、前端测试/类型检查/构建及Compose配置全部通过。
- 脱敏Lyra夹具解析结果与人工基准一致：70条规格、22名成员、24个节点、3个计划版本。
- PostgreSQL实际执行 `0001_phase1` 升级成功，API容器加载模板清单及Excel/数据库依赖成功。
- Phase 2后端36个测试、Ruff、mypy、依赖一致性检查全部通过；管理端2个测试、TypeScript检查和生产构建通过。
- SQLite迁移完成 `base -> 0002 -> base` 往返验证；保留Phase 1数据的PostgreSQL完成 `0001 -> 0002` 增量升级。
- Docker真实闭环完成“创建项目 -> 上传Lyra规格书 -> MinIO保存 -> 发布v1 -> 查看看板”，结果为70条规格、22名成员、24个节点。
- 管理端浏览器验证总览、版本历史、问题与审计页面正常，控制台无错误。
- Phase 3后端44个测试、管理端4个测试、小程序2个测试，以及Ruff、mypy、TypeScript和生产构建全部通过。
- SQLite完成 `base -> 0003 -> base` 往返迁移；保留Phase 2数据的PostgreSQL完成 `0002 -> 0003` 增量升级，共13张含Alembic的表。
- Docker真实闭环完成“R成员绑定 -> M23完成提案 -> A成员审批 -> 发布v2”，并验证问题登记、消息中心和自然语言预填。
- 管理端浏览器验证v2看板、问题记录、成员绑定和待审批列表正常。

## 计划里程碑

| Tag | 范围 | 状态 |
|-----|------|------|
| v0.1.0 | Phase 0：工程与文档基线 | ✅ 已完成（2026-08-06） |
| v0.2.0 | Phase 1：领域模型与固定模板解析 | ✅ 已完成（2026-08-06） |
| v0.3.0 | Phase 2：后台核心与管理端 | ✅ 已完成（2026-08-06） |
| v0.4.0 | Phase 3：微信小程序 | ✅ 已完成（2026-08-06） |
| v0.4.1 | Phase 3.1：一致性、安全与协作闭环补强 | ✅ 已完成（2026-08-07） |
| v0.5.0 | Phase 4：自动提醒与外部通知 | ✅ 已完成（2026-08-07） |
| v1.0.0 | Phase 5：Lyra真实试点和首次生产可用 | 🔄 进行中（未创建Tag） |

> `v0.1.0`用于标记Phase 0工程基线；`v0.2.0`用于标记Phase 1固定模板解析基线；`v0.3.0`用于标记Phase 2后台核心闭环；`v0.4.0`用于标记Phase 3小程序协同闭环；`v0.4.1`用于标记Phase 3.1一致性与安全补强。自Phase 2起，每阶段自测和diff审核通过后按用户授权自行提交并创建Tag，不自动push。
