# Phase 3 验收记录

## 验收范围

- 微信登录会话、项目邀请、手机号绑定及手机号不一致人工审核。
- 未绑定用户项目隔离；R成员仅能提交本人负责节点；A或项目经理审批与驳回。
- 小程序项目列表、看板、节点更新、问题登记、消息中心和自然语言预填。
- 管理端成员绑定审核与变更提案审批。
- OpenAI兼容Chat Completions请求和严格JSON Schema契约。

## 自动化结果

- 后端：44个pytest测试通过，Ruff格式与规则检查、mypy检查通过。
- 管理端：4个Vitest测试、TypeScript检查和生产构建通过。
- 小程序：2个Node测试和TypeScript检查通过。
- SQLite迁移：`base -> 0003 -> base` 往返通过。
- Docker镜像：API 0.4.0和管理端镜像构建通过。

## PostgreSQL与API真实链路

保留Phase 2数据执行 `0002_phase2 -> 0003_phase3` 增量迁移成功，数据库共有13张含Alembic的表。基于Lyra Pro项目执行：

1. 邀请并绑定“成员10”（R），移动端看板读取v1和24个节点。
2. R提交M23完成提案，正式版本保持v1，提案状态为pending。
3. 邀请并绑定“成员09”（A），由A审批提案后发布v2。
4. v2中M23实际完成日期为 `2026-08-06`。
5. 移动端登记问题成功；自然语言“`M23延期到2026-08-30`”预填为M23、结束日期 `2026-08-30`，并要求用户确认。
6. 消息中心返回成员绑定通知。

## 管理端浏览器验收

- 总览显示Lyra Pro当前版本v2、24个节点和1个未关闭问题。
- 问题页显示Docker链路创建的问题。
- 成员绑定页显示成员09、成员10均已绑定。
- 已审批提案不再出现在待审批列表。

## 外部联调边界

本机未安装微信开发者工具，项目也尚未配置真实AppID/AppSecret，因此本阶段使用受配置开关保护的模拟微信登录完成自动化与Docker验收。真实 `wx.login`、手机号动态凭证和微信审核包需在取得小程序测试环境后复验；生产环境必须关闭模拟登录。
