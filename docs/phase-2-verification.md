# Phase 2 验收记录

## 验收范围

- 项目创建与项目级成员权限。
- Lyra固定模板上传、解析报告、字段级差异、取消和显式发布。
- 不可变项目版本、重复文件去重、乐观并发冲突与事务回滚。
- 进度变更提案、问题管理、审计日志和写操作幂等。
- 管理端总览、导入、版本历史、问题与审计页面。
- PostgreSQL增量迁移与MinIO原始文件存储。

## 自动化结果

| 检查项 | 结果 |
|--------|------|
| 后端Pytest | 36 passed |
| Ruff格式与静态检查 | 通过 |
| Mypy严格类型检查 | 通过 |
| Python依赖一致性 | 通过 |
| 管理端Vitest | 2 passed |
| 管理端TypeScript检查 | 通过 |
| 管理端生产构建 | 通过 |
| SQLite迁移往返 | `base -> 0002 -> base` 通过 |
| PostgreSQL增量迁移 | `0001 -> 0002` 通过 |

## Docker端到端结果

2026-08-06使用脱敏Lyra测试规格书完成：

1. 创建项目 `ZPD1322 / Lyra Pro`。
2. 上传并解析 `.xlsx`，原文件写入MinIO。
3. 查看差异并发布为项目版本v1。
4. 看板返回70条产品规格、22名成员、24个里程碑。
5. 浏览器确认总览、版本历史、问题与审计页面正常，控制台无错误。

## 已知非阻塞项

- 管理端生产构建仍提示Ant Design公共依赖块超过500 kB；业务页面已经懒加载，不影响Phase 2功能验收，后续可按访问性能数据继续拆分公共依赖。
- `X-Actor-Id`仅用于Phase 2开发身份模拟，正式微信身份认证属于Phase 3范围。
