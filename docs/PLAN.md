# AI项目管理系统实施计划

## 1. 实施原则

**Phase 1–6审查补强（2026-08-10）**：移动端写入以服务端绑定身份为权威；审批与驳回共享数据库锁；展示日期由后台业务日期驱动；外部微信入口生成不占用业务事务；客户端未知网络结果仅使用同一幂等键重试。

- 严格以 `docs/PRD.md` 为产品基线，规格变化先更新PRD版本记录。
- 先定义测试，再实现功能；每个Phase验收通过后才能进入下一Phase。
- MVP只实现固定格式 `.xlsx`，不提前实现任意Excel解析。
- 解析器输出统一内部模型，核心业务不依赖Excel坐标或LLM供应商。
- 所有阈值、地址、模型和通道开关进入外部配置，不写死在代码中。
- 每个Phase完成全部自测和diff审核后，按用户授权自行提交并创建对应里程碑tag；不自动push。

## 2. 开源方案调研结论

| 项目 | 可借鉴内容 | 本项目处理方式 |
|------|------------|----------------|
| [Plane](https://github.com/makeplane/plane) | 工作项、周期、模块、状态和看板的信息组织 | 借鉴交互和领域概念，不引入其完整平台 |
| [OpenProject](https://github.com/opf/openproject) | 工作包、角色、项目计划、通知及审计 | 借鉴权限和变更追踪思路，不复用其大型单体架构 |
| [java-json-tools/json-patch](https://github.com/java-json-tools/json-patch) | RFC 6902的`test/add/replace/remove`及基于原始文档生成差异 | 借鉴结构化操作和发布前目标值复核；不允许客户端传入任意JSON路径 |
| [Microsoft REST API Guidelines](https://github.com/microsoft/api-guidelines) | ETag/If-Match乐观并发及标准HTTP冲突语义 | 当前保留显式`base_version_number`以兼容已有客户端，过期写入统一返回409 |

两者均是成熟通用项目管理平台，但本项目的差异化核心是“企业规格书导入 + 项目模板版本 + AI提案 + 微信个人触达”。MVP采用自建轻量领域模型，避免为了少量功能引入大型平台及许可证、升级和二次开发成本。进入具体模块实施前仍需对拟复用代码逐项检查许可证和维护状态。

## 3. 技术方案

### 3.1 技术栈

| 层级 | 选择 | 用途 |
|------|------|------|
| 后端 | Python 3.12、FastAPI、Pydantic、SQLAlchemy、Alembic | API、校验、领域逻辑和数据库迁移 |
| 数据库 | PostgreSQL | 项目版本、关系数据、审计和通知记录 |
| 异步任务 | Redis、Celery | Excel解析、每日扫描、消息发送与重试 |
| 管理后台 | React、TypeScript、Ant Design | 导入、差异确认、配置和审计 |
| 小程序 | 微信原生小程序、TypeScript | 登录绑定、看板、更新和消息中心 |
| 文件存储 | S3兼容对象存储（生产）/ MinIO（本地） | 原始Excel、导入报告和导出物 |
| AI | OpenAI Python SDK兼容客户端 + 内部适配层 | 标准传输、结构化输出和供应商切换 |
| 部署 | Docker Compose（开发/试点），公司VPC内生产环境 | 可重复部署和数据隔离 |

### 3.2 仓库建议结构

```text
apps/
  api/              # FastAPI
  admin-web/        # 管理后台
  mini-program/     # 微信小程序
packages/
  schemas/          # API契约/生成产物
config/
  app.example.yaml
docs/
tests/
  fixtures/         # 脱敏后的Lyra模板和变体
tmp/                # 不提交的临时文件
```

避免在MVP阶段拆分微服务。API和Worker共享同一领域代码，以部署进程区分Web请求与异步任务。

### 3.3 后端模块

- `identity`：用户、openid、手机号和项目邀请绑定。
- `projects`：项目、版本、成员、规格、里程碑和RACI。
- `imports`：模板识别、解析器注册、校验、差异和发布。
- `changes`：Excel和小程序变更提案、冲突和审批。
- `issues`：重难点问题及处理记录。
- `notifications`：规则计算、消息中心、微信和短信适配器。
- `llm`：OpenAI兼容客户端、Schema校验、提示词版本和审计。
- `audit`：不可变操作日志。

### 3.4 核心接口草案

| 方法与路径 | 用途 |
|------------|------|
| `POST /api/v1/projects` | 创建项目 |
| `POST /api/v1/projects/{id}/imports` | 上传 `.xlsx` 并创建导入任务 |
| `GET /api/v1/imports/{id}` | 查看解析、校验和差异结果 |
| `POST /api/v1/imports/{id}/publish` | 确认并发布导入版本 |
| `GET /api/v1/projects/{id}/dashboard` | 项目看板数据 |
| `POST /api/v1/milestones/{id}/progress-proposals` | 提交进度/完成/延期提案 |
| `POST /api/v1/change-proposals/{id}/approve` | 审批变更 |
| `GET /api/v1/projects/{id}/editable-data` | 读取后台可编辑快照和稳定资源键 |
| `POST /api/v1/projects/{id}/change-sets` | 创建多操作变更集和差异预览 |
| `GET /api/v1/projects/{id}/change-sets` | 查询项目变更集 |
| `GET /api/v1/change-sets/{id}` | 查询变更集详情 |
| `POST /api/v1/change-sets/{id}/publish` | 二次校验并原子发布新版本 |
| `POST /api/v1/change-sets/{id}/cancel` | 取消未发布变更集 |
| `POST /api/v1/projects/{id}/issues` | 登记问题 |
| `PATCH /api/v1/issues/{id}` | 更新问题 |
| `DELETE /api/v1/issues/{id}` | 管理后台版本化关闭问题并记录原因 |
| `DELETE /api/v1/mobile/issues/{id}` | 小程序版本化关闭本人负责的问题 |
| `POST /api/v1/auth/wechat/login` | 微信登录和绑定 |
| `GET /api/v1/me/notifications` | 我的消息中心 |

写接口使用幂等键和乐观并发版本号。幂等键同时绑定规范化请求摘要，并发重复请求必须返回同一结果而不是唯一约束错误；问题更新使用数据库条件更新避免丢失写入。接口契约在实现前以OpenAPI和测试固化。

### 3.5 解析与发布流水线

```text
文件校验
  -> 模板指纹/元数据识别
  -> ParserRegistry选择解析器
  -> CanonicalProjectDraft
  -> Pydantic/业务校验
  -> 与当前ProjectVersion进行语义Diff
  -> 冲突处理
  -> 人工确认
  -> 事务发布ProjectVersion
```

解析器不能直接访问正式表执行写入。发布服务是唯一可以更新项目当前版本的入口。

### 3.6 外部配置

至少包含以下配置，并在示例文件逐项说明：

- 数据库、Redis和对象存储地址。
- Excel最大尺寸、允许扩展名、ZIP条目/解压上限和隔离进程导入超时。
- `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`、超时、指数退避参数、并发和结构化输出模式。
- 微信AppID、密钥、订阅消息模板ID。
- 腾讯云短信凭证、签名、模板ID和地区。
- 时区、工作日历、每日/每周扫描时间、临期天数和升级天数。
- 通知通道开关、短信降级事件和每日发送上限。

## 4. 测试先行清单

以下用例应在对应实现代码之前建立。涉及真实手机号和微信凭证的测试使用沙箱或Mock。

### 4.1 Excel解析测试

| 编号 | 用例 | 预期 |
|------|------|------|
| XLSX-01 | 导入Lyra基准模板 | 识别模板1.0并得到完整规范模型 |
| XLSX-02 | 解析24个节点和计划版本 | 最后一条变更计划成为候选当前计划 |
| XLSX-03 | 空白与N/A | 分别得到TBD与NOT_APPLICABLE |
| XLSX-04 | 单日与日期区间 | 日期类型和边界正确 |
| XLSX-05 | RACI公式引用 | 解析为正确成员，不受公式缓存错误影响 |
| XLSX-06 | 缺少必要Sheet/表头 | 明确失败位置，不产生草稿发布 |
| XLSX-07 | `.xls`或伪装扩展名 | 拒绝上传 |
| XLSX-08 | 未知模板版本 | 拒绝并返回支持版本列表 |

### 4.2 版本与冲突测试

| 编号 | 用例 | 预期 |
|------|------|------|
| VER-01 | 相同文件重复导入 | 无业务差异，不创建重复正式版本 |
| VER-02 | Excel修改节点日期 | 展示字段级差异，确认后新版本生效 |
| VER-03 | 小程序先修改、旧Excel后导入同字段 | 标记冲突，不静默覆盖 |
| VER-04 | 取消导入 | 正式数据不变 |
| VER-05 | 发布中途异常 | 事务回滚，无部分版本 |
| VER-06 | 并发审批同一提案 | 仅一个请求成功 |
| VER-07 | 后台变更集新增、修改和删除规格 | 预览差异后原子发布新版本 |
| VER-08 | 同一变更集移除成员及RACI引用 | 全部成功或全部回滚，不产生悬空引用 |
| VER-09 | 使用过期基线创建或发布变更集 | 返回409，当前版本不变 |
| VER-10 | 取消变更集 | 正式版本不变，审计可追溯 |
| VER-11 | 小程序作废本人负责的问题或尝试作废他人问题 | 本人问题版本化关闭并记录原因；他人问题返回403 |

### 4.3 提醒测试

| 编号 | 用例 | 预期 |
|------|------|------|
| NTF-01 | 未来3天到期 | 生成站内临期提醒 |
| NTF-02 | 当天到期且无订阅授权 | 站内消息存在，按配置短信降级 |
| NTF-03 | 节点逾期 | 每个业务日按升级规则生成提醒 |
| NTF-04 | 节点完成/延期获批 | 旧提醒规则停止或按新日期计算 |
| NTF-05 | 任务重复执行 | 幂等键阻止重复通知 |
| NTF-06 | 微信发送失败 | 记录错误、重试并按策略降级 |
| NTF-07 | 问题解决 | 停止问题追踪提醒 |

### 4.4 AI契约测试

| 编号 | 用例 | 预期 |
|------|------|------|
| AI-01 | 供应商支持JSON Schema | 返回值通过本地Schema校验 |
| AI-02 | 供应商仅支持JSON文本 | 本地提取和校验成功 |
| AI-03 | 非法JSON/缺少字段 | 有限重试后失败，不写正式数据 |
| AI-04 | 超时或限流 | 指数退避且错误可诊断 |
| AI-05 | 切换base_url和model | 业务代码及Schema不变 |
| AI-06 | 输入包含手机号 | 发给模型前被排除或脱敏 |

### 4.5 权限与审计测试

- 非项目成员不能访问项目。
- R只能更新授权节点，不能发布Excel版本。
- R/A/C/I均不能通过小程序API通用增删产品规格、成员或RACI。
- A只能审批本人负责节点的提案，项目经理可确认并发布后台变更集。
- 手机号和openid在日志及普通响应中脱敏。
- 每次修改可定位操作者、来源、前值、后值和项目版本。

### 4.6 管理后台交互测试

| 编号 | 用例 | 预期 |
|------|------|------|
| WEB-01 | 产品规格选择20/50条每页 | 当前页条数立即生效，不被默认值覆盖 |
| WEB-02 | 在规格、成员、里程碑/RACI标签新增或修正 | 打开对应字段表单，变更原因必填 |
| WEB-03 | 删除被引用成员 | 显示受影响RACI，未一并处理时不允许发布 |
| WEB-04 | 提交后查看差异 | 显示操作类型、资源、前值、后值、原因和基线版本 |
| WEB-05 | 确认发布或取消 | 发布后刷新为新版本；取消不修改正式数据 |

## 5. 分阶段实施

### Phase 0：工程与文档基线

**当前状态（2026-08-06）**：已完成。工程、本地测试、容器健康检查和管理端浏览器渲染均已通过，可在首次提交确认后进入Phase 1。

**工作内容**

- 初始化Git前再次确认仓库边界，不自动提交。
- 建立后端、后台、小程序目录和统一开发命令。
- 建立配置示例、代码质量工具、测试框架和CI。
- 准备脱敏Lyra测试夹具；原始含手机号文件不得提交公共仓库。

**验收标准**

- 本地一条命令启动依赖并运行空项目。
- lint、类型检查和测试命令可重复执行。
- `.env`、`tmp/`和原始敏感文件不会被版本控制收录。

**建议里程碑**：`v0.1.0`，创建tag前必须征得用户确认。

### Phase 1：领域模型与固定模板解析

**当前状态（2026-08-06）**：已完成并通过验收，里程碑为 `v0.2.0`。

**工作内容**

- 先实现XLSX-01至XLSX-08测试和脱敏夹具。
- 建立数据库模型、迁移和CanonicalProjectDraft Schema。
- 实现模板1.0指纹、ParserRegistry和LyraTemplateV1Parser。
- 实现语义校验、Diff和导入报告。

**验收标准**

- Lyra脱敏夹具解析结果与人工基准100%一致。
- 24个节点、计划版本、空白/N/A、RACI和输出物全部通过测试。
- 无效文件不产生正式项目数据。

**建议里程碑**：`v0.2.0`。

### Phase 2：后台核心与管理端

**工作内容**

- 先实现版本、冲突、权限和审计测试。
- 实现项目、成员、规格、计划、问题和变更API。
- 实现上传、差异确认、版本发布和历史查看页面。
- 实现项目看板和问题管理。

**验收标准**

- 项目经理可完成“创建项目 -> 导入 -> 查看差异 -> 发布 -> 查看看板”。
- Excel与人工更新冲突必须显式处理。
- 关键写操作均有审计记录，权限测试通过。

**建议里程碑**：`v0.3.0`。

**当前状态（2026-08-06）**：已完成并通过验收，里程碑为 `v0.3.0`。

### Phase 3：微信小程序

**当前状态（2026-08-06）**：已完成开发环境闭环并通过验收，里程碑为 `v0.4.0`。真实微信联调仍需外部提供AppID、AppSecret和微信开发者工具，不阻塞本阶段的接口、权限与模拟登录验收。

**工作内容**

- 先实现登录绑定、项目隔离和更新授权测试。
- 实现微信登录、邀请、手机号绑定和异常审核。
- 实现我的项目、看板、节点更新、问题和消息中心。
- 实现自然语言预填接口；无法使用LLM时保留结构化表单。

**验收标准**

- 未绑定用户无法查看项目。
- R可提交本人节点完成或延期，正式更新符合审批规则。
- 小程序页面与后台当前已发布版本一致。

**建议里程碑**：`v0.4.0`。

### Phase 3.1：一致性、安全与协作闭环补强

**当前状态（2026-08-07）**：已完成并通过验收，里程碑为 `v0.4.1`。

**工作内容**

- 以失败测试复现后台身份伪造、旧基线提案发布、历史Excel假发布、冲突状态回滚、非法日期和成员权限残留。
- 管理后台改用服务端Bearer认证；手机号改为HMAC摘要与掩码存储。
- 分离原文件哈希和业务快照哈希，补齐项目行锁、提案基线/目标值校验和导入冲突持久化。
- 发布Excel后重整成员绑定与项目权限，并限制同一用户在同一项目只能绑定一个成员身份。
- 管理端增加邀请生成；小程序增加A审批、问题更新和消息已读，并修复跨项目残留状态。
- 实现OpenAI兼容重试、严格Schema失败后的JSON模式降级及输入敏感信息脱敏。

**验收标准**

- 未携带有效管理员Bearer token的管理API全部拒绝访问。
- 旧基线提案、历史Excel和非法日期不能产生新正式版本；导入冲突状态可查询。
- 仅重新保存但业务未变化的Excel差异为零。
- 团队移除成员后旧用户立即失去访问；重复身份绑定被拒绝。
- 从管理端创建邀请到小程序绑定、R提交、A审批、问题更新和消息已读可完成端到端操作。
- 手机号数据库与API均不出现明文；LLM供应商不支持严格Schema时可按配置降级并仍执行本地校验。

**建议里程碑**：`v0.4.1`。

### Phase 4：自动提醒与外部通知

**状态**：✅ 已完成（2026-08-07）。

**工作内容**

- 首先增加AES-GCM手机号密文及密钥版本字段；保留HMAC用于匹配、掩码用于展示，仅通知服务可在短信发送时临时解密。
- 先实现NTF-01至NTF-07测试和时间边界测试。
- 实现每日/每周扫描、升级、幂等和通知记录。
- 接入微信订阅消息和腾讯云短信适配器。
- 实现发送失败、重试、降级、限额和后台诊断。

**验收标准**

- 使用可控测试时钟验证3天、当天、逾期和升级规则。
- 重跑任务不重复发送。
- 微信不可用时站内消息不丢失，关键事件按配置降级。

**建议里程碑**：`v0.5.0`。

### Phase 5：Lyra真实试点与生产准备

**状态**：🔄 进行中（2026-08-07）。已完成真实文件隔离导入、生产配置检查、运行监控及备份恢复演练；等待项目经理人工核对、正式通知通道和连续两周试运行。

**可靠性补强（2026-08-10）**：已完成导入子进程隔离与资源限制、请求摘要幂等、问题原子乐观锁、通知发送前持久化、批量扫描查询、微信token缓存、LLM退避、业务时区统一及管理端生产镜像/拆包。

**工作内容**

- 在隔离环境导入真实Lyra文件并由项目经理核对。
- 邀请试点成员，验证授权、更新、提醒和问题闭环。
- 执行备份恢复、安全检查、监控告警和操作手册演练。
- 根据试点结果修正规则，不扩展任意Excel解析范围。

**当前执行结果**

- 真实Lyra文件已在隔离环境完成解析和发布：70条产品规格、19名唯一成员、24个里程碑、3个计划版本；同一人员的多个岗位合并为单一成员及多角色。
- 当前计划包含3个待定节点，需项目经理结合项目台账确认；真实姓名和手机号不写入仓库或验收文档。
- PostgreSQL与MinIO已完成备份、校验和一次性环境恢复演练；新增API/worker/beat健康检查和通知运行状态告警。
- 管理端“项目核对”可查看当前正式版本的70条规格、19名脱敏成员及多角色、24个里程碑的R/A责任关系，并筛选3个待定节点；既支持Excel重导入，也支持后台变更集修正并发布新版本。
- 正式微信、订阅模板、腾讯云短信配置及连续两周试运行尚未完成，因此不得标记Phase 5完成或创建`v1.0.0`。

**验收标准**

- 连续至少两周无丢提醒、重复关键提醒或静默覆盖。
- 抽查规格、人员、节点和审计记录与人工台账一致。
- 完成回滚、备份恢复和通知通道故障演练。

**建议里程碑**：`v1.0.0`。

### Phase 5.1：多端版本化数据维护

**状态**：🔄 进行中（2026-08-07）。PRD、迁移、API、后台与小程序交互及自动化回归已完成；待项目经理登录后完成真实数据人工核对，并确认里程碑提交与Tag。

**工作内容**

- 新增变更集持久化，保存基线版本、来源、操作列表、差异、原因、状态和提交/发布人。
- 建立受限的`add/replace/remove`应用器，只允许操作产品规格、成员、里程碑、计划和RACI的已知字段与稳定资源键。
- 创建、发布和取消变更集均使用幂等键；发布时锁定项目并重新校验基线、目标值和引用完整性。
- 管理后台“项目核对”增加新增、修正、删除、变更原因、差异预览和确认发布交互。
- 小程序继续复用受权限约束的节点提案和问题接口，不暴露规格、成员和RACI通用写入能力。
- 更新审计页和版本历史，可区分Excel导入、后台表单和小程序提案来源。

**实施顺序与阶段验收**

1. 迁移、Schema和变更集领域服务 → 验证：VER-07至VER-10、权限反向测试通过。
2. 后台API与OpenAPI契约 → 验证：新增/修改/删除、查询、发布、取消和409冲突全部通过。
3. 项目核对编辑与差异交互 → 验证：WEB-02至WEB-05组件测试和真实浏览器验收通过。
4. 小程序回归与三通道并发 → 验证：R/A/C/I权限、Excel重导入冲突和通知重算通过。
5. 全量迁移、API、后台、小程序、Docker联合验收 → 验证：无部分版本、无静默覆盖、无越权写入。

**建议里程碑**：`v0.6.0`，创建Tag前必须征得用户确认。

### 小程序UI优化阶段

**状态**：✅ 已完成（2026-08-11）。各阶段已独立完成实现与自动化验收，提交仍按阶段确认。

1. 看板操作、卡片密度、重复标题和全局安全区统一 → 验证：关键操作使用一致操作行，小屏首屏展示更多有效信息。
2. 问题列表优先与登记表单按需展开 → 验证：进入页面无需滚动即可看到现有问题，并保持新增、编辑闭环。
3. 节点更新表单与自然语言预填分层 → 验证：字段标签、确认状态和提交操作始终清晰。
4. 团队成员与RACI检索、长内容展开 → 验证：规格、成员和节点均可快速定位。
5. 错误重试、消息未读、导航图标和真机细节 → 验证：主要空态、错误态和小屏安全区通过真机检查。

### 小程序项目风险概览增强

**状态**：✅ 已完成（2026-08-12）。

1. 将近期节点窗口改为业务日期当天至未来14天（含边界）→ 验证：当天、第14天纳入，第15天排除。
2. 看板固定展示待办、近期、逾期，将已完成和全部收纳到更多筛选 → 验证：主要风险入口无需横向滑动即可访问。
3. 移动项目列表接口返回当前版本节点、计划、RACI和后台业务日期 → 验证：仅绑定项目可访问，节点数据不包含联系方式。
4. 项目首页展示近期与逾期节点及完整RACI → 验证：逾期使用红色语义，近期使用橙色语义，点击项目仍可进入看板。

### RACI个人任务、问题协同与跨端看板

**状态**：🔄 进行中（2026-08-12）。Phase A规格与现状审计、Phase B小程序“我的”任务、Phase C问题RACI与每日状态、Phase D Web项目看板已完成，后续按阶段实施并分别验收、提交。

#### Phase A：规格与现状审计

- 明确“我的”只聚合当前成员承担R/A的里程碑，按项目分类且沿用统一风险口径。
- 明确问题责任人为R，新增A/C/I，并保留现有业务状态；风险分类由预计完成日期派生。
- 确认现有每日通知扫描已覆盖里程碑和问题，14天小程序风险窗口与默认3天通知提前量保持独立配置。
- 确认管理后台尚无按项目任务看板，需要新增项目级聚合视图。

**验收**：PRD版本、角色语义、状态派生、通知边界、阶段顺序与测试要求完整记录。

#### Phase B：小程序“我的”任务

**状态**：✅ 已完成（2026-08-12）。

1. 先增加移动接口与页面契约测试，覆盖R、A、R+A去重、无任务和跨项目隔离。
2. 扩展移动端任务聚合接口，返回本人角色、计划日期、风险分类和项目摘要。
3. 增加“我的”底部导航与按项目分组页面，支持待办/近期/逾期/已完成筛选及进入项目看板。

**验收**：仅展示当前绑定成员R/A任务；分类边界与项目看板一致；四项底部导航在窄屏和安全区下无溢出。

#### Phase C：问题RACI与每日状态

**状态**：✅ 已完成（2026-08-12）。

1. 先增加模型、迁移、Schema、服务层和通知回归测试，覆盖必填A、可选C/I、成员约束、状态派生及提醒接收人。
2. 增加问题RACI持久化与API，兼容既有责任人字段作为R并提供历史数据迁移。
3. 更新管理后台和小程序问题表单、列表与详情，展示R/A/C/I及待办/近期/逾期/已完成分类。
4. 扩展现有每日扫描接收人规则，不新建重复定时任务；通知提前量继续读取外部配置。

**验收**：迁移可往返；旧问题不丢责任人；已解决/关闭停止提醒；R/A收到约定提醒；C/I不会被默认轰炸；并发和审计测试通过。

**验证结果**：问题A必填及R/A/C/I正式成员约束、14天风险边界、移动端RACI重分配限制、到期时R+A提醒及C/I默认不提醒均有回归覆盖；SQLite完成全迁移链往返和旧问题R到A的数据回填验证；API 106项、管理端14项、小程序51项测试及Python/TypeScript类型检查通过。

#### Phase D：Web项目看板

**状态**：✅ 已完成（2026-08-12）。

1. 先增加项目看板API契约和管理端组件测试，覆盖项目隔离、四类筛选、节点RACI与问题摘要。
2. 新增管理后台项目看板路由和项目入口，复用统一风险分类服务，避免前后端分别计算日期边界。
3. 接入现有变更、审批和问题操作入口，不绕过权限、乐观锁和审计。

**验收**：Web与小程序同一业务日期下分类结果一致；项目经理可从项目列表进入；无跨项目数据泄露。

**验证结果**：管理后台增加独立项目看板入口，按服务端待办/近期/逾期/已完成分类展示节点计划与完整RACI，并展示当前项目问题风险摘要；看板可跳转既有项目数据变更和问题管理入口。API与小程序共用里程碑风险函数，14天边界有回归覆盖；API 108项、管理端15项、小程序51项测试及Python/TypeScript检查通过。

#### Phase E：联合验收与文档收口

- 执行API、迁移、通知、管理端、小程序全量回归，并使用真实项目数据核对R/A个人任务、问题风险和跨端看板。
- 更新OpenAPI、运维说明、CHANGELOG和TodoList；API或管理端产物变化时重建Docker服务并完成浏览器/真机冒烟。

**验收**：三项需求端到端通过，风险分类无端间偏差，通知无重复或漏停；提交和Tag仍按用户确认执行。

### 项目经理权限与重点问题审批闭环

**状态**：🔄 进行中（2026-08-12）。

#### Phase F1：项目经理身份与节点审批权限

**状态**：✅ 已完成（2026-08-12）。

- 导入时校验团队表有且仅有一名项目经理，并在正式快照中保留规范化身份。
- 绑定与重新发布时按团队表自动授予、转移项目经理权限，保持节点RACI不变；迁移修复既有绑定权限。
- 节点审批禁止提交人自审；R=A时仅项目经理可审，R≠A时节点A或项目经理任一可审。

**验收**：缺失/重复经理导入失败；经理换岗后权限自动转移；自审失败且独立A/项目经理审批成功；审计与历史版本不受损。

#### Phase F2：任意成员R与重点问题新增审批

- 小程序登记问题时R可选择任意正式成员；新增先形成申请，批准前不进入正式业务数据与通知。
- 小程序项目经理和Web管理员可批准、驳回并留下审计记录。

#### Phase F3：重点问题删除审批

- 删除改为申请；待审期间原问题有效，批准后版本化关闭并停止提醒，驳回保持不变。

#### Phase F4：小程序、Web审批界面与通知

- 补齐新增/删除审批入口、状态反馈、权限空态与审批结果消息；通知扫描仅覆盖已批准有效问题。

#### Phase F5：迁移、联合验收与文档收口

- 迁移历史问题为已批准，完成API、迁移、通知、Web与真机联合验收并更新运维文档。


## 6. Phase间联合验收

- Phase 2完成后重新执行Phase 1全部导入回归测试。
- Phase 3完成后验证小程序更新与Excel重导入的冲突闭环。
- Phase 4完成后验证计划发布、完成确认、延期和问题状态对提醒的即时影响。
- Phase 5执行全量回归、权限测试、备份恢复和敏感数据检查。
- Phase 5.1完成后重新执行Excel导入、小程序提案、通知重算和历史版本回归，确认三个变更来源共用相同的冲突与审计语义。

## 7. 后续路线

### 7.1 标准化数据Sheet

新增 `StandardDataParser` 和标准字段字典。展示Sheet可变化，但标准化Sheet及版本保持稳定。验收重点是不同展示模板产生相同Canonical模型。

### 7.2 AI任意格式导入

新增 `AIImportParser`，输出字段映射、置信度、缺失项和冲突。所有结果进入人工确认；建立多类型历史规格书评测集后，达到约定准确率才开放使用。

### 7.3 其他集成

根据真实使用数据再评估企业微信内部群、邮件、日历、PLM/ERP和数据分析集成，不在MVP预留无实际调用的复杂代码。

## 8. 开始实施前需要的外部条件

- 微信小程序AppID、合法域名和主体权限。
- 订阅消息模板审核结果。
- 腾讯云短信签名、模板和测试额度。
- 公司VPC、PostgreSQL、Redis和对象存储环境。
- 可用于开发的脱敏Lyra Excel夹具。
- 首个OpenAI兼容LLM服务的测试密钥、base URL和模型名。
