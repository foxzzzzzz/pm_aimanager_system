# Phase 3.1 验收记录

验收日期：2026-08-07

## 范围

- 8项代码审查缺陷：管理身份伪造、旧提案误批准、历史Excel假发布、冲突状态回滚、倒置日期、成员权限残留/重复绑定、协作入口缺失、源文件哈希伪差异。
- 优化项：手机号隐私、发布并发锁、管理端跨项目状态/请求竞态、LLM兼容降级和小程序行为测试。

## 自动化验收

执行 `scripts/check.ps1`：

- Ruff：通过。
- mypy：29个源文件通过。
- 后端：54项通过。
- 管理端：7项通过。
- 小程序：4项通过，其中2项为真实请求器行为测试。
- 管理端和小程序TypeScript检查：通过。
- 管理端生产构建：通过。
- Docker Compose配置检查：通过。

## PostgreSQL与Docker验收

- 五个Compose服务均为healthy。
- PostgreSQL完成 `0003_phase3 -> 0004_phase31` 增量迁移。
- 独立临时数据库以Phase 3旧快照和旧手机号播种后升级，确认旧版本内容哈希已转换为业务快照哈希。
- `mobile_users` 与 `member_bindings` 不再包含手机号明文字段，仅保留HMAC和掩码字段。
- 跨数据库掩码表达式使用 `length()-3`，实际结果为 `138****0010`、`138****0009`。
- 管理API：无令牌返回401，错误令牌返回403，有效令牌返回200。

## 浏览器验收

- 未认证访问管理端时显示不可跳过的管理员认证窗口。
- 输入有效运行时令牌后可加载Lyra Pro v2看板。
- 问题与审计页可进入成员绑定页签，手机号仅显示掩码，并显示“生成邀请”入口。

## 结论

Phase 3.1验收通过，可创建 `v0.4.1` 里程碑。真实微信AppID联调仍属于外部环境事项，不影响本阶段本地协作闭环验收。
