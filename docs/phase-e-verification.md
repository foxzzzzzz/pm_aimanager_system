# Phase E 联合验收记录

> 2026-08-13更新：本页保留Phase E既有自动验收证据；人工验收已合并到Phase F5完成，其中M01-M07通过，M08外部通知配置进入TodoList。

## 2026-08-12 自动验收

- 执行 `scripts/check.ps1`：Ruff、mypy、Docker Compose配置检查通过；API 109项、管理端15项、小程序51项测试通过；管理端与小程序TypeScript检查以及管理端生产构建通过。
- 修复Windows系统临时目录不可访问导致的Pytest环境故障；验收脚本现在按项目规范使用根目录 `tmp/pytest`，并增加脚本回归测试。
- 在重建服务前生成PostgreSQL与MinIO备份：`tmp/backups/20260812-145623-399`。该目录仅用于本地恢复，不进入Git。
- 重建API、通知worker、通知beat和管理端镜像，执行 `scripts/start-backend.ps1`；7个Compose服务全部健康，PostgreSQL迁移至 `0008_issue_raci (head)`。
- 在独立SQLite数据库上完成空库升级至head、回退至0007、再次升级至head，迁移链通过。
- 真实PostgreSQL数据中，问题表RACI三列存在，1条旧问题不存在空RACI字段；管理端项目看板接口当时返回24项任务（逾期1、近期1、待办22）及1项近期问题。待办统计包含“不适用”节点的问题已在Phase F5修复。
- 对3个本地移动用户执行个人任务服务校验，其中已绑定且承担任务的用户按项目返回1项任务，角色仅包含R和A，无其他角色混入。
- OpenAPI包含移动端个人任务、移动端项目看板和管理端项目看板路径；问题新增、更新模型均公开R/A/C/I字段。
- 通知自动化回归覆盖每日扫描幂等、到期、逾期升级、已解决/已完成停止提醒、问题R+A接收且C/I不接收高频提醒；运行态无投递失败和滞留待处理。
- 管理端未登录页面可正常加载并显示“项目看板”入口和管理员令牌门禁；敏感令牌未写入浏览器自动化日志。

## 已移交Phase F5的人工项目

1. **真机小程序**：确认底部“我的”入口、按项目分组、四类风险筛选、R/A角色标签和进入项目看板的布局、点击及返回均正常。
2. **业务语义**：在当前真实项目中，确认“我的”仅出现当前成员应承担的R/A任务；项目看板排除“不适用”节点后的逾期1、近期1、待办16及问题近期1与业务预期一致。
3. **问题闭环**：用小程序登记一条测试问题（R、A必填，C/I可选），确认小程序与Web显示一致；将其改为已解决或已关闭后，确认归入已完成。
4. **Web登录后页面**：输入本地管理员令牌，确认项目看板四类筛选、节点RACI、问题风险摘要和跳转入口在目标浏览器中无错位或溢出。
5. **真实通知渠道**：配置并授权正式微信订阅消息后，确认R/A收到预期提醒、C/I不收到高频提醒、同一日重扫不重复、问题解决后不再提醒。短信只有在渠道完成备案与配置后再验收。

## 最终结论

Phase E自动验收通过。上述第1至4项已在Phase F5联合验收中完成，第5项外部通知配置已进入TodoList，不阻塞本地功能收口。Phase E作为验收工具与历史证据归档完成。
